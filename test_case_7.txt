    [* this is comment for this sample code 
         for assignment 3 *]
[*if-else case *]
    $$ 
    int      i, max, sum;   [* declarations *]
    sum = 0;
    i = 1;
    get ( max);
    if (i <  max){  
          sum = sum + i;
          i  = i + 1; 
	}
    else
	sum =10;

    ifend
     put (sum + max);
     $$
